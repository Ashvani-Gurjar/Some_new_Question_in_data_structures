#include<bits/stdc++.h>
using namespace std;
int main(){
     //   string s;
     //   getline(cin,s); // Getline is used to take all values in stirng 
     //   cout<<s<<endl;
//     string s1="fam";
//     string s2="ily";
//     s1.append(s2); // Append is used to put value to other string and arrays.

//     cout<<s1<<endl;
//     cout<<s1+s2<<endl;
//  1 clear() is used to eriese all data in string 
//  2 compare() is used to compare data in two stirng 
//  3  append() is used to push data to other string 
//  4  empty() is used to cheak string is empty or not 
 // 5  erase() is used to delete characters in strings
 // 6  find() is used to find value in strings.
 // 7  insert() is used to insert in string .   
 // 8  length() is used to measure the lenght of string
 // 9   substr() is used to prient the substring of the string 
//  10  stoi() is used to covert string to integer
//  11  to_string is used to covert interger to string
//  12   sort(.begin(),.end()) is used to sort a string 



     //  string s1="Ashvanisingh";
     //  string s2="fams";
     //  if(s2.compare(s1)==0){
     //      cout<<"String is equal";
     //  }
     //  else{
     //      cout<<"String is not equal";
     //  }
     // s1.erase(3, 5);
     // cout<<s1<<endl;
     // cout<<s1.find("vani")<<endl;
     
//     s1.insert(3,"gurjar");

//     string s=  s1.substr(3,6);
    
//     cout<<s<<endl;

     // string s ="454";
     // int x=stoi(s);
     // cout<<x+3<<endl;
    
     // int x=3434;
     // cout<<to_string(x).append("43")<<endl;

      string s="dfslklksieurwk";
      sort(s.begin(),s.end());
      cout<<s<<endl;

return 0;
}